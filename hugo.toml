baseURL = 'https://JanLudva.github.io/267112_ZPC_2025/'
languageCode = 'cz-cs'
title = 'ZPC'
theme = 'ananke'
# Use absolute/canonical URLs so assets (CSS/JS/images) reference the site root
# on GitHub Pages. Relative URLs caused paths like "./267112_ZPC_2025/..." which
# broke loading of the theme CSS/JS when the site is served from the project path.
relativeURLs = false
canonifyURLs = true
publishDir = "public"


[menu]

  [[menu.main]]
    identifier = "about"
    name = "O mně"
    url = "/about/"
    weight = 10

    [[menu.main]]
    identifier = "project"
    name = "Projekt"
    url = "/project/"
    weight = 20

    [[menu.main]]
  identifier = "smaller-projects"
  name = "Dílčí projekty"
  url = "/smaller-projects/"
  weight = 30
  
  [[menu.main]]
  identifier = "smaller-projects"
  name = "Dílčí projekty"
  url = "/smaller-projects/"
  weight = 30
  
  

  # Allow raw HTML in content (enables iframe/raw HTML). This is required because
  # Hugo's Goldmark renderer omits raw HTML by default for safety.
  [markup]
    [markup.goldmark]
      [markup.goldmark.renderer]
        unsafe = true

